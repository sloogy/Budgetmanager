description = "Generiert 3 Conventional-Commit Varianten aus staged diff."
prompt = """
Erstelle 3 Conventional-Commit Messages basierend auf staged changes.

```diff
!{git --no-pager diff --staged}
```

Format:
type(scope): summary
- body bullets (optional)
"""
