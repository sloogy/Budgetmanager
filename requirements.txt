# BudgetManager — direkte Laufzeit-Abhängigkeiten
# Für Releases immer das vollständig gehashte requirements.lock verwenden.

PySide6==6.10.3          # Qt für Python - UI Framework
openpyxl==3.1.5         # Excel-Export/Import
cryptography==50.0.0    # Fernet: AES-128-CBC + HMAC-SHA256
requests==2.34.2        # HTTPS-Updateprüfung
packaging==26.2         # Versionsvergleich
defusedxml==0.7.1       # Schutz vor XML-Entitäten/DTD
