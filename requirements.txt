# BudgetManager — Laufzeit-Abhängigkeiten
# Dev-Tools: requirements-dev.txt | Build-Tools: requirements-build.txt

PySide6>=6.6.0          # Qt für Python - UI Framework
openpyxl>=3.1.0         # Excel-Export Funktionalität
cryptography>=41.0.0    # AES-256 Fernet Verschlüsselung (Benutzerkonten/verschlüsselte DBs)
requests>=2.31.0        # HTTP-Requests für Update-Prüfung
packaging>=23.0         # Version Parsing
